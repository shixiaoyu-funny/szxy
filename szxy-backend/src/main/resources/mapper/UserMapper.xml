<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shixiaoyu.xiangyueproject.mapper.UserMapper">
    <insert id="comment">
        INSERT INTO user_comment (
            user_id,
            target_type,
            target_id,
            content,
            score,
            comment_img,
            create_time,
            is_show
        )
        VALUES (
                   #{userId},
                   #{targetType},
                   #{targetId},
                   #{content},
                   #{score},
                   #{commentImg},
                   now(),
                   #{isShow}
               )
    </insert>

    <!-- 1. 插入点赞：必须包含 user_id 字段 -->
    <insert id="insertLike">
        INSERT INTO user_like (user_id, target_id, create_time)
        VALUES (#{userId}, #{targetId}, #{createTime})
    </insert>

    <!-- 2. 检查点赞：必须根据用户 ID 过滤 -->
    <select id="checkLike" resultType="java.lang.Integer">
        SELECT count(*) FROM user_like
        WHERE user_id = #{userId} AND target_id = #{targetId}
    </select>

    <!-- 3. 删除点赞：必须根据用户 ID 删除 -->
    <delete id="deleteLike">
        DELETE FROM user_like
        WHERE user_id = #{userId} AND target_id = #{targetId}
    </delete>

    <!-- 更新景点表 village_scenic 的 likes 字段 -->
    <update id="updateScenicLikeCount">
        UPDATE village_scenic
        SET likes = likes + #{step}
        WHERE id = #{scenicId}
    </update>

    <!-- 1. 检查收藏 -->
    <select id="checkCollect" resultType="java.lang.Integer">
        SELECT count(*) FROM user_collect
        WHERE user_id = #{userId} AND target_id = #{targetId}
    </select>

    <!-- 2. 插入收藏 -->
    <insert id="insertCollect">
        INSERT INTO user_collect (user_id, target_id, create_time)
        VALUES (#{userId}, #{targetId}, #{createTime})
    </insert>

    <!-- 3. 删除收藏 -->
    <delete id="deleteCollect">
        DELETE FROM user_collect
        WHERE user_id = #{userId} AND target_id = #{targetId}
    </delete>

    <!-- 4. 更新景点表 village_scenic 的 collections 字段 -->
    <update id="updateScenicCollectCount">
        UPDATE village_scenic
        SET collections = collections + #{step}
        WHERE id = #{scenicId}
    </update>
</mapper>