<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 注意：Namespace 必须指向 ManagerAccessMapper -->
<mapper namespace="com.shixiaoyu.xiangyueproject.mapper.ManagerAccessMapper">

    <!-- A. 降权：把旧人改回农户 -->
    <update id="demoteOldManager">
        UPDATE farm_user
        SET type = 1
        WHERE user_id = #{oldManagerId} AND village_id = #{villageId}
    </update>

    <!-- B. 提权：把新人升为村长 -->
    <update id="promoteNewManager">
        UPDATE farm_user
        SET type = 2
        WHERE user_id = #{userId} AND village_id = #{villageId}
    </update>

    <!-- 查询村长资质申请列表（联查用户信息） -->
    <select id="selectManagerAccessList" resultType="com.shixiaoyu.xiangyueproject.entity.vo.ManagerAccessVO">
        SELECT
            ma.id,
            ma.user_id AS userId,
            u.username AS username,
            u.phone AS phone,
            ma.village_id AS villageId,
            ma.farm_name AS farmName,
            ma.id_card AS idCard,
            ma.status,
            ma.type
        FROM manager_access ma
                 JOIN user u ON ma.user_id = u.id
        ORDER BY ma.id DESC
    </select>
</mapper>